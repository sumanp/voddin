<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h5><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h5>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.check_box :done, class: "filled-in"%>
    <%= f.label :done, id: 'done' %><br>

  </div>
  <div class="field">
    
    <%= f.collection_select :project_id, Project.all, :id, :title %>
    <%= f.label :project_id, id: 'project_id' %> <br>
  </div><br>
  <div class="actions">
    <%= f.submit class:"btn" %>
  </div>
<% end %>
